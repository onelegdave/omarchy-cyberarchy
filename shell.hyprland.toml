# Shared border tokens. Menus, popups, tooltips, notifications, the launcher,
# polkit and the lock screen all reference these, so setting both here gives
# every shell surface the same purple border.
[hyprland]
active-border            = "#6c7fe0"
active-border-foreground = "#6c7fe0"
